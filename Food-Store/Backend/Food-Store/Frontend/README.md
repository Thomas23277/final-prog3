# Food-Store Frontend (improved)

Cambios aplicados automáticamente:
- Corregido enlace a CSS (`/src/style.css` -> `/src/styles.css`).
- Agregado `adminHome.ts` para proteger la ruta de administrador y añadir logout.
- Eliminado `node_modules` del paquete final para reducir tamaño (ejecuta `npm install` antes de `npm run dev`).
- Mantener backend en `http://localhost:8080` como en `src/utils/auth.ts`.

Instrucciones:
1. Desde `Food-Store/Frontend` ejecutar `npm install`.
2. `npm run dev` para correr Vite (entrada: `index.html`).
